- if devise_mapping.recoverable? && controller_name != 'passwords'

  = link_to "Olvidaste tu contraseña?", new_password_path(resource_name)

- if devise_mapping.recoverable? && controller_name != 'passwords'
  = link_to "Olvidaste tu contraseña?", new_password_path(resource_name)
  %br/
- if devise_mapping.confirmable? && controller_name != 'confirmations'
  = link_to "¿No recibiste instrucciones para confirmar?", new_confirmation_path(resource_name)
  %br/
- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
  = link_to "¿No recibiste instrucciones para desbloquear?", new_unlock_path(resource_name)
  %br/
- if devise_mapping.omniauthable?
  - resource_class.omniauth_providers.each do |provider|
    = link_to "Conectar con #{provider.to_s.titleize}", omniauth_authorize_path(resource_name, provider)
    %br/
