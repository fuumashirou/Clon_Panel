!!! 5
%html
  %head
    %meta{ charset: "utf-8" }
    %meta{ name: "application-name", content: "Twable" }
    %title= content_for?(:title) ? yield(:title) : "Display Twable"
    = stylesheet_link_tag :display, media: "all"
  %body
    = yield
