!!! 5
%html
  %head
    %meta{ charset: "utf-8" }
    %meta{ name: "application-name", content: "Twable" }
    %title= content_for?(:title) ? yield(:title) : "Boleta"
    = stylesheet_link_tag "/static/print", media: "all"
  %body
    = yield
